{
  "name": "ge-pulse-app",
  "version": "1.0.0",
  "description": "GE Pulse Frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && npx esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic --format=iife --target=es6 --loader:.ts=tsx && cp index.html dist/index.html && sed -i 's|type=\"module\" src=\"/index.tsx\"|src=\"/bundle.js\" defer|g' dist/index.html && cp metadata.json dist/metadata.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.2",
    "node-fetch": "^2.6.7",
    "ts-node": "^10.9.1"
  }
}